{
  "name": "sanitizeuri",
  "version": "1.0.0",
  "description": "sanitize uri",
  "main": "index.js",
  "scripts": {
    "build": "babel -d . ./src",
    "dev": "nodemon -w ./src --exec \"npm run build\"",
    "prebuild": "for i in $(ls ./src); do rm -fr $i; done",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "sanitize",
    "uri"
  ],
  "author": "Steven Enten <steven@enten.fr>",
  "license": "MIT",
  "dependencies": {},
  "engines": {
    "node": ">=5.3.0",
    "npm": ">=3.5.3"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "nodemon": "^1.9.2"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/enten/node-sanitizeuri.git"
  },
  "bugs": {
    "url": "https://github.com/enten/node-sanitizeuri/issues"
  },
  "homepage": "https://github.com/enten/node-sanitizeuri"
}
